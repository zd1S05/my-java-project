<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.csv.mapper.ShippingMethod3Mapper">
	
	<select id="selectById" resultType="ShippingMethod3">
		SELECT  * FROM shipping_method3
		WHERE id = #{id}
	</select>
	
</mapper>